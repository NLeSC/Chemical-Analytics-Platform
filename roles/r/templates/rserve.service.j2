[Unit]
Description=Rserve - Server providing R functionality to applications via TCP/IP or local unix sockets
After=network.target remote-fs.target nss-lookup.target

[Service]
Type=forking
User={{ rserve_user }}
ExecStart=/usr/bin/R CMD Rserve --vanilla

[Install]
WantedBy=multi-user.target
